import React, { useEffect, useState } from "react";
import io from "socket.io-client";

const Chat = ({ onNewMessage }) => {
  return (
    <div>
      <h2>Chat UI</h2>

    </div>
  );
};

export default Chat;
